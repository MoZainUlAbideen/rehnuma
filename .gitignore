# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
build/

# Secrets
.env

# REAL BILL PHOTOS CONTAIN PII (reference no., consumer ID, address) — never commit
data/real/*
!data/real/.gitkeep
!data/real/README.md

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# NEPRA PDFs: public but large; re-download with `rehnuma-policy fetch` (hashes in sources.lock.json)
data/policy/raw/
data/policy/embeddings/